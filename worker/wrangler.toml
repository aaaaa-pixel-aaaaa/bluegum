name = "bluegum"
main = "src/index.ts"
compatibility_date = "2024-09-23"

# Created with `wrangler kv namespace create BLUEGUM_KV` — paste the id it
# prints back in here. Not secret, safe to commit.
[[kv_namespaces]]
binding = "BLUEGUM_KV"
id = "d1944fcee0324ceeb9baf15442bce9db"

# Secrets (TMDB_READ_TOKEN, ANTHROPIC_API_KEY, APP_SECRET) are set with
# `wrangler secret put <NAME>` — never listed here, never committed.
